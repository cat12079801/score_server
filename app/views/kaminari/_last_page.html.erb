<% if current_page.last? %>
  <li class="disabled">
    <a style="cursor: default"><%= raw(t 'views.pagination.last') %></a>
  </li>
<% else %>
  <li>
    <%= link_to raw(t 'views.pagination.last'), url, {:remote => remote} %>
  </li>
<% end %>
